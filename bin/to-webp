#!/bin/bash
for i in *.png
do
    t=$(echo $i|sed -e 's/\.png/\.webp/')
    if [ -e "$i" ]; then
        magick "$i" "$t"
    fi
done
for i in *.jpg
do
    t=$(echo $i|sed -e 's/\.jpg/\.webp/')
    if [ -e "$i" ]; then
        magick "$i" "$t"
    fi
done
